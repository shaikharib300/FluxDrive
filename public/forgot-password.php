<?php
require_once __DIR__.'/../includes/functions.php';
$message=null;
if($_SERVER['REQUEST_METHOD']==='POST'){ verify_csrf($_POST['_csrf']??null); $email=strtolower(trim($_POST['email']??'')); $st=db()->prepare('SELECT id FROM users WHERE email=? LIMIT 1'); $st->execute([$email]); $u=$st->fetch(); if($u){$token=bin2hex(random_bytes(32));$hash=hash('sha256',$token);$exp=date('Y-m-d H:i:s',time()+3600);db()->prepare('UPDATE password_resets SET used_at=NOW() WHERE user_id=? AND used_at IS NULL')->execute([$u['id']]);db()->prepare('INSERT INTO password_resets(user_id,token_hash,expires_at) VALUES(?,?,?)')->execute([$u['id'],$hash,$exp]); $message='Reset link: '.rtrim(envv('APP_URL','http://localhost/FluxDrive/public'),'/').'/reset-password.php?token='.$token;} else $message='If the account exists, a reset link would be sent.';}
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Forgot password — FluxDrive</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="../assets/css/style.css"></head><body class="auth-page"><div class="auth-card"><a class="brand mb-4" href="index.php"><span class="brand-mark"><i class="bi bi-cloud-arrow-up"></i></span>FluxDrive</a><h1>Reset password</h1><p class="muted">Enter your email. In local development the reset link is shown here.</p><?php if($message):?><div class="alert alert-info"><?=e($message)?></div><?php endif;?><form method="post"><input type="hidden" name="_csrf" value="<?=e(csrf_token())?>"><input class="form-control form-control-lg mb-3" type="email" name="email" required placeholder="you@example.com"><button class="btn btn-primary btn-lg w-100">Create reset link</button></form><p class="text-center mt-4"><a href="login.php">Back to login</a></p></div></body></html>
